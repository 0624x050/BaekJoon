#include <stdio.h>

int main(){
    int T;              // 테스트 개수
    int H, W, N;        // 호텔 층, 방의 수, 몇 번째
    scanf("%d", &T);
    
    for(int i=0; i<2; i++){
        scan("%d %d", &H, &W, &N);
    }
    return 0;
}